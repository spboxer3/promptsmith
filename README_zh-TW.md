<p align="center"><img src="assets/icons/icon128.png"></p>

<h1 align="center">PromptSmith - AI 驅動的最佳化客戶端 (BYOK)</h1>

<p align="center"><a href="README.md">English</a> | 繁體中文</p>

PromptSmith 是一款注重隱私、「自帶金鑰 (Bring Your Own Key, BYOK)」的 Chrome 擴充功能。它讓您可以直接在瀏覽器中，使用您偏好的 AI 模型（OpenAI、Anthropic、Gemini 或本地端模型）來優化文字、修正語法和微調提示詞 (Prompt)，適用於任何網頁。

## 核心特色

- **自帶金鑰 (BYOK)：** 支援連接您自己的 OpenAI、Anthropic 或 Google Gemini API 金鑰。完全掌控您的使用量與成本。
- **支援本地端 AI：** 可無縫連接至本地運行的 LLM（例如透過 LM Studio, Ollama, LocalAI），確保絕對隱私與離線功能。
- **網域白名單：** 控制擴充功能在哪些網站上啟用。預設支援主流 AI 工具（ChatGPT、Claude、Gemini 等），並支援自訂網域與萬用字元。
- **全網通用：** 適用於任何網站。選取文字，喚醒 PromptSmith，立即優化。
- **自訂策略 (Strategies)：** 針對特定任務建立專屬提示詞策略，例如「修正語法」、「專業語氣」、「摘要重點」或「程式碼審閱」。
- **預設端點：** 設定全域預設端點，讓多個策略共用。一處修改，全局生效。
- **隱私均先：** 您的 API 金鑰與設定皆儲存於您的瀏覽器本地端，不經過任何第三方中介伺服器。
- **智慧介面：** 極簡的懸浮審閱視窗、差異 (Diff) 比對檢視，並支援鍵盤快捷鍵 (`Alt+P`) 快速啟動。

## 安裝指南

1.  複製此專案代碼或下載原始碼壓縮檔。
2.  開啟 Chrome 瀏覽器並前往 `chrome://extensions`。
3.  開啟右上角的 **開發人員模式 (Developer mode)**。
4.  點擊 **載入未封裝項目 (Load unpacked)**。
5.  選擇包含此擴充功能檔案的目錄。

## 使用說明

1.  **設定 (Configuration)：**

    - 點擊擴充功能圖示或進入 **設定 (Options)** 頁面。
    - 前往 **連線端點 (Endpoints)** 新增您的 AI 服務商資訊 (API Key, 模型名稱)。
    - (選用) 設定 **優化策略 (Strategies)** 來客製化 AI 處理文字的方式。

2.  **優化文字：**
    - 在網頁上選取任意文字。
    - 按下觸發快捷鍵 (預設為 `Alt+P`)，或點擊懸浮的 "PromptSmith" 按鈕 (若已啟用)。
    - 從選單中選擇一個策略。
    - 在差異檢視視窗中審閱 AI 的建議。
    - 點擊 **套用變更 (Apply)** 直接替換原始文字，或點擊 **複製 (Copy)** 將結果存入剪貼簿。

## 隱私條款

[隱私條款](Privacy%20Policy.md)

## 更新日誌

### v0.5.2

- **錯誤修正**：修復載入狀態下 FAB 重複建立導致懸浮圖示重複出現的問題。
- **錯誤修正**：修復 observer 清理後殘留的 debounce timer 導致懸浮圖示位置漂移的問題。
- **錯誤修正**：將 FAB 定位從 `absolute` 改為 `fixed`，修正因座標系統不一致導致的圖示錯位。
- **效能優化**：對 selection handler 加入 debounce 機制，避免每次按鍵都重建懸浮按鈕。
- **程式碼清理**：合併重複的 `clearFabs()` 方法為單一強制清理實作。

### v0.5.1

- **介面優化**：統一浮動選單與設定頁面的下拉選單樣式，呈現一致的高質感視覺。
- **篩選器改進**：將歷史紀錄篩選器移至更易存取的位置，優化版面配置。
- **多語系修正**：修正歷史紀錄搜尋欄位的翻譯缺失。
- **版面修正**：修正浮動選單中圖示對齊問題。

### v0.5.0

- **歷史紀錄功能 (History)**：自動在本地端儲存您最近 7 天的優化紀錄。
- **全新視覺介面**：在設定頁面中新增「歷史紀錄」分頁，採用現代化卡片設計，支援搜尋、預覽原始/優化對照及一鍵複製。
- **快速存取**：在浮動選單新增時鐘圖示，可快速呼叫歷史紀錄視窗。


### v0.4.1

- **快速白名單管理**：直接在 Popup 介面中將當前網站加入或移除白名單。支援管理自訂網域以及還原被移除的預設支援網域。

### v0.4.0

- **預設端點功能：** 設定全域預設端點，可讓多個策略共用。輕鬆在一處切換您偏好的 AI 模型。
- **自訂下拉選單：** 重新設計的端點選擇器，採用漸層標籤樣式，視覺更清晰。
- **星號按鈕：** 端點列表中使用 ★/☆ 圖示快速設定預設端點。
- **自動轉移：** 刪除預設端點時，自動將預設轉移至下一個可用端點。

### v0.3.10

- **錯誤修正**：修正匯入設定檔時，預設「Make Prompt Better」策略遺失端點設定的問題。

### v0.3.9

- **懸浮按鈕清理 (FAB Cleanup)**：修復「幽靈圖示」問題，當輸入框從 DOM 移除時（如 SPA 切換對話），懸浮按鈕會自動消失。
- **錯誤處理優化**：優化 Console 日誌記錄並增加對孤兒腳本的防護，避免 Chrome 誤報錯誤。
- **錯誤修正**：修正 Localhost 白名單驗證支援，以及 i18n 翻譯時保留按鈕圖示。

### v0.3.7

- **Enter 鍵攔截**：當審閱視窗 (Review UI) 或載入中狀態開啟時，阻止 Enter 鍵觸發底層網頁的表單送出。

### v0.3.6

- **權限與政策優化**：優化 Manifest 權限設定（移除 `activeTab`, `contextMenus`），並更新隱私條款以說明 `scripting` 權限用途。

### v0.3.5

- **白名單優先**：將白名單檢查移至最早時機，非白名單網域不會執行任何 chrome API 呼叫。
- **監聽器清理**：修正孤兒 `chrome.storage.onChanged` 監聽器導致 context invalidated 錯誤的問題。

### v0.3.4

- **介面修正**：修復策略列表中因文字過長導致按鈕錯位的版面問題。

### v0.3.3

- **熱修復重載 (Hot-fix)**：擴充功能重載時自動重新注入腳本並清理舊實例，無需重新整理網頁即可繼續使用。

### v0.3.2

- **預設分類更新**：更新預設分類為 General, Image, Writing, Coding
- **快捷鍵修正**：修正重複按下快捷鍵導致選單錯誤的問題
- **智慧分類**：內建策略現在有預設分類，且用戶可自定義覆寫

### v0.3.1

- **分類篩選器**：在設定頁面依分類篩選策略

### v0.3.0

- **策略分類**：將策略整理至不同分類，便於管理
- **階層式選單**：分類展開子選單，清晰顯示策略
- **拖曳排序**：可拖曳調整分類順序
- **完整鍵盤操作**：使用方向鍵、Enter 及 Escape 導航選單
- **分類標籤顯示**：策略列表中顯示所屬分類
- **Toast 確認對話框**：改用 Toast 風格的刪除確認

### v0.2.1

- **鍵盤導航**：使用方向鍵和 Enter 操作策略選單
- **懸浮按鈕修正**：僅在輸入框有內容時顯示

### v0.2.0

- **網域白名單**：針對特定網域啟用或停用擴充功能
- **進階變數**：在策略模板中使用 JavaScript 變數

## 授權條款

MIT
